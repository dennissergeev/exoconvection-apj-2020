To recreate the conda environment exactly, install [conda-lock](https://github.com/conda/conda-lock) and run the following command.

```bash
conda-lock install --name sergeev20_plot3d conda-lock.yml
```
